name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm docs:audit
      - run: pnpm typecheck
      - run: pnpm content:validate
      - run: pnpm safety:scan
      - run: pnpm test
      - name: Build explicitly labelled draft preview
        run: pnpm build:preview
        env:
          ENABLE_GPT56: "false"
          NEXT_PUBLIC_DEMO_MODE: "true"
          NEXT_PUBLIC_ENABLE_TREATMENT_SAVING: "false"
          ALLOW_DRAFT_CONTENT: "ARIAD_EXPLICIT_UNREVIEWED_PREVIEW"
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Run browser and accessibility checks
        run: pnpm test:e2e
        env:
          NEXT_PUBLIC_ENABLE_TREATMENT_SAVING: "false"
